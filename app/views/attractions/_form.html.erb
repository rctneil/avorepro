<%= form_with(model: attraction) do |form| %>
  <% if attraction.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(attraction.errors.count, "error") %> prohibited this attraction from being saved:</h2>

      <ul>
        <% attraction.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :venue_id, style: "display: block" %>
    <%= form.text_field :venue_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
