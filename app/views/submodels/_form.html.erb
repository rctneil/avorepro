<%= form_with(model: submodel) do |form| %>
  <% if submodel.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(submodel.errors.count, "error") %> prohibited this submodel from being saved:</h2>

      <ul>
        <% submodel.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :model_id, style: "display: block" %>
    <%= form.text_field :model_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
